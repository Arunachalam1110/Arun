create database final;
use ado;

create table orderdetails( orderid varchar(100), sno varchar(20),productid varchar(30), productname varchar(20),
  price varchar(20), quantity varchar(20), orderdate varchar(50)) 
     
	 select * from orderdetails